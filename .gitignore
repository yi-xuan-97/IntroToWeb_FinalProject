# dependencies
/node_modules

# environment variables (contains EmailJS credentials — never commit)
.env
.env.local

# OS / editor cruft
.DS_Store

# build output
/build

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
